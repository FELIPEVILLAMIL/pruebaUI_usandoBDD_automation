I assume you should have python on your machine, if not, please install it.
please execute: pip install -r requirements.txt
run the test with: behave --junit -f behave_html_formatter:HTMLFormatter -o ./reports/report1.html -f pretty features

